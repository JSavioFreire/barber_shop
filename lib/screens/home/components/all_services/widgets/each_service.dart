import 'package:flutter/material.dart';

class EachService extends StatelessWidget {
  const EachService({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 50,
      height: 50,
      color: Colors.red,
    );
  }
}
